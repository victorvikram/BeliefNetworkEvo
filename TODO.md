# Project Timeline

## Backlog

### Code Quality
- [ ] Refactor `clean_raw_data.py` — extract repeated transform pattern in `transform_regular()` into a helper; simplify DataConfig
- [ ] Extract inline HTML/JS from `temporal_network_visualizer.py` into a template file

### Analysis
- (add planned analyses here)

### Infrastructure
- (add planned infra work here)
